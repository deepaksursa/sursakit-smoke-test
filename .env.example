# 🔧 Test Environment Configuration Template
# Copy this file to .env and update with your actual values

# Base URL for testing
BASE_URL=http://localhost:5173

# Test Credentials
# Replace with your actual test credentials
TEST_USERNAME=your_email@example.com
TEST_PASSWORD=your_password

# Browser Configuration
HEADLESS=false

# Environment
NODE_ENV=development

# Optional: CI/CD Configuration
CI=false

# Optional: Debugging
DEBUG=false
MAILSLURP_API_KEY=yourKey


# Instructions for Junior Developers:
# 1. Copy this file: cp .env.example .env
# 2. Update TEST_USERNAME and TEST_PASSWORD with real credentials
# 3. Change BASE_URL if testing against different environment
# 4. Set HEADLESS=true for background testing
# 5. Never commit the .env file to git (it's in .gitignore)
